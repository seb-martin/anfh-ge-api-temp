# Interface Utilisateur
ui:
  build: ./ui

# Interface Applicative
api:
  build: ./api
  links:
    - db:db

# Base de données
db:
  build: ./db

# Gestion
mgmt:
  build: ./mgmt
  environment:
    - ELASTICSEARCH_URL=http://db:9200
  links:
    - db
